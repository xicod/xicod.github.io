Debian gui system upgrade (run as root):

(crontab -u $(id -nu 1000) -l 2>/dev/null | grep -v DT_SYSTEM_UPGRADE; echo '1 11 * * 1 echo DT_SYSTEM_UPGRADE; DISPLAY=:0 gnome-terminal --maximize -- bash -c "echo Enter your password to start system upgrade; echo; sudo bash -c \"export NEEDRESTART_SUSPEND=1; apt-get update && apt-get -V dist-upgrade -y && apt-get -V autoremove --purge && apt-get clean && needrestart -m e -r a -k -l\"; echo Press Enter to close the window; read"') | crontab -u $(id -nu 1000) -
